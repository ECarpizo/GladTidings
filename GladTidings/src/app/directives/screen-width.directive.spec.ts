import { ScreenWidthDirective } from './screen-width.directive';

describe('ScreenWidthDirective', () => {
  it('should create an instance', () => {
    // const directive = new ScreenWidthDirective();
    // expect(directive).toBeTruthy();
  });
});
